ADZUNA_APP_ID=seu_app_id_real
ADZUNA_APP_KEY=sua_app_key_real

GEMINI_API_KEY=sua_gemini_api_key

AWS_ACCESS_KEY_ID=sua_access_key_id
AWS_SECRET_ACCESS_KEY=sua_secret_access_key
AWS_DEFAULT_REGION=us-east-1
# Preferido pelo script de custo de vida; Adzuna usa AWS_S3_BUCKET_NAME.
AWS_BUCKET_NAME=canadapt-data-lake
AWS_S3_BUCKET_NAME=canadapt-data-lake

# GitHub Actions (Settings → Secrets and variables → Actions):
# ADZUNA_APP_ID, ADZUNA_APP_KEY, GEMINI_API_KEY,
# AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_DEFAULT_REGION,
# AWS_BUCKET_NAME, AWS_S3_BUCKET_NAME
# Workflow: .github/workflows/weekly_pipeline.yml (segundas 12:00 UTC)
#
# Streamlit Community Cloud (Secrets da app) — só leitura da Gold:
# AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_DEFAULT_REGION,
# AWS_BUCKET_NAME, AWS_S3_BUCKET_NAME
# Modelo: .streamlit/secrets.toml.example

