name: 'canadapt'
version: '1.0.0'
config-version: 2
profile: 'canadapt_profile'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  canadapt:
    staging:
      +materialized: view
    marts:
      +materialized: table
